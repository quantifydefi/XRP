{
  "family": "${CONTAINER_NAME}",
  "containerDefinitions": [
    {
      "name": "${CONTAINER_NAME}",
      "image": "${IMAGE_NAME}",
      "environment": [],
      "essential": true,
      "hostname": "${CONTAINER_NAME}",
      "portMappings": [
        {
          "hostPort": 0,
          "protocol": "tcp",
          "containerPort": 3000
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "qc-defi-nuxt-server-${RUN_ENV}",
          "awslogs-region": "us-west-2",
          "awslogs-stream-prefix": "qc-defi-nuxt-server"
        }
      }
    }
  ],
  "requiresCompatibilities": [
    "EC2"
  ],
  "cpu": "128",
  "memory": "512"
}
